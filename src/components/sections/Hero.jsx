export default function Hero({title, subtitle}){
    return (
        <section>
            <h1>{title}</h1>
            <p>{subtitle}</p>
        </section>
    );
}



// Tranformar a Hero para manipular al view User:
//  - definir el bloque para manipular el estado de un contador

